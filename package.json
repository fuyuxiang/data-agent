{
  "name": "meridian-analytics-workbench",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 app.py",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check-vendor.mjs && node --check frontend/src/app.js && node --check frontend/src/api.js && node --check frontend/src/components.js && node --check frontend/src/analysis-panel.js && node --check frontend/src/product-panel.js && node --check frontend/src/panels.js",
    "test:browser": "npm run build && playwright test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
