在当前 data-agent 仓库直接实施完整改造，不要只回复方案。

先读取适用的 AGENTS.md，检查 git status、实际 HEAD 与工作区已有修改，再完整阅读我提供的 CODEX_DATA_AGENT_FINAL_V3.md。该文件是唯一实施规范，完整替代旧版任务书与历史聊天中的不一致要求。配套 DATA_AGENT_FINAL_ARCHITECTURE_V3.md 只作说明；全部必交条款已包含在主文件。

目标：自研 Agent 循环和运行层，不引入 Echo、Pi、LangGraph 等运行底座；接入 PB/EB 级现有数仓，大计算留在数仓/分布式平台；完整实现需求书业务无关的功能，包括一次四段需求确认、范围与文件、动态过程、三类成果、四指标/四图、两个 Word/四图 PNG、真实邮件、历史、记忆/Skills 和增量追问。

同时必须完成代码收敛与清理。保留必要功能和用户数据，不等于保留旧实现。按主文件 §2.5 和 R21 建立 FEATURE_SCOPE、CLEANUP_MANIFEST、清理及测试映射。合并聊天/团队的重复循环、工具与状态，删除已替换路径；核验后退役范围外能力及其工具注册、UI、依赖、资源、配置、文档和打包入口。不要把旧代码移到 legacy/archive、注释、隐藏开关后冒充清除。

主规范包含 R01–R21、F01–F34、A01–A64、W01–W30、C01–C24；全部必交。沿用并增强有效测试，已退役功能专属测试可以随决议移除，但必须记录理由与替代断言，不能削弱安全/恢复/功能验收。不要通过 TESTING 放开权限，不要 fake success，不要容器故障后回退主进程执行生成代码。

审查基线是 6f977e57fb821d0c2ec9c5810881c9c9a2f875d4；当前版本不同则先核对差异，禁止强制回退或覆盖用户修改。路径和符号是审查入口，不是盲删清单。动态路由、工具/Job/Skill 注册、CLI、迁移、fixture、第三方许可证和历史数据必须纳入检查。普通代码清理不授权删除上传文件、凭据、远端表、历史成果或迁移链。

继续使用现有 Flask/Waitress/Vue/本地资源策略，在原产品中接通唯一正式内核和 DatasetRef/远端执行。旧 API 必要时仅保留有消费者、测试和退役条件的薄兼容层，不保留第二套执行逻辑。参考 Trino/Iceberg/Spark-Livy 路径必须真实实现并锁定兼容版本；目标数仓未知不代表参考环境已经证明生产兼容或 PB/EB 性能。

维护 docs/advanced-data-agent/ 的 SPEC、PLAN、STATUS、ACCEPTANCE、DECISIONS、FEATURE_SCOPE、CLEANUP_MANIFEST、CLEANUP_REPORT、CAPABILITIES 和规模记录。按 M0–M6 持续编码、迁移、删除、测试、修复；每个阶段都同步清理，不完成第一阶段后把其他要求改为“以后扩展”。中断前保存精确进度和下一步，恢复后从文件事实续接。

区分 NOT_IMPLEMENTED 与已实现但测试 BLOCKED；缺模型凭据、容器、集群或目标权限时如实记录并继续其他工作，禁止将未运行验收记 PASS。未经授权不得扫描生产大仓、写业务源数据、向真实用户发邮件或推送发布。

最终报告真实代码变化、删除/合并清单、依赖与产物变化、保留的兼容及期限、迁移/启动/验证命令、R/F/A/W/C 到测试与日志的映射、实际结果与阻塞。功能完成、参考分布式通过、清理完成、目标平台接入、PB/EB 实测必须分别报告。必要实现缺失、旧路径残留或必测未通过时，不得宣称完整完成。

现在开始实际检查、实现、清理与验证。
