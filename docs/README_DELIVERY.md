# 最终交付包 V3

版本日期：2026-09-06。

本包交付的是交给 Codex 的完整实施规范和技术方案，**不包含已经改造的仓库源码，也不证明仓库测试或 PB/EB 性能已经通过**。

## 文件

- `CODEX_DATA_AGENT_FINAL_V3.md`：唯一完整任务书。包含 R01–R21 工程要求、F01–F34 原需求功能、A01–A64 核心场景、W01–W30 大仓场景和 C01–C24 清理场景。可直接全文交给 Codex，不依赖旧文件或原聊天。
- `DATA_AGENT_FINAL_ARCHITECTURE_V3.md`：配套架构与代码取舍说明，便于负责人审阅；不另增第二套要求。
- `CODEX_DATA_AGENT_FINAL_START.txt`：将主任务书放入目标仓库根目录或可读附件后使用的启动指令。
- `SHA256SUMS.txt`：本包文本文件的校验和。

## 使用

主任务书完整替代此前 FULL_IMPLEMENTATION、WAREHOUSE_V2、旧覆盖矩阵及旧启动指令；不要多版本同时作为有效任务。

保留当前仓库实际 HEAD 与用户修改，不回退到审查基线。将主任务书提供给 Codex，粘贴启动文本。方案文件可选提供，主任务书已经包含全部功能、架构、清理和测试合同。

“清理”指有证据地移除废弃源码及关联资源，不是删除用户数据、所有历史迁移或所有 mock 测试。完整性按功能与验收检查，不按新增/删除的代码行数。
