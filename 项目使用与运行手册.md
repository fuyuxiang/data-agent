# 经纬分析工作台（Meridian）使用手册（非运行版）

这份文档只讲**“怎么用”**，不包含安装、启动命令。  
目标：让你知道每个页面/功能先做什么、干什么、为什么、做完后下一步去哪儿。

## 一、先建立统一上下文（任何功能前都要先做）

在所有场景里，先确认这三件事：

- **登录状态/角色是否可用**  
  作用：系统里所有数据和配置都是按“会话 + 工作空间”隔离。  
  怎么用：右上角个人头像区可退出/恢复会话；未登录会先进入登录/注册页。  
  下一步：进入一个可用的工作空间后再进行数据与分析操作。

- **当前工作空间是谁**（左上角下拉）  
  作用：不同空间对应不同的资产、权限、配额和日志。  
  怎么用：从工作空间切换器选中目标空间；会话、数据源、知识、分析结果会随空间切换更新。  
  下一步：确保你在要交付给哪个客户/事业部的空间里再继续。

- **当前会话是否已选数据源**（侧边会话 + 数据选择）  
  作用：智能分析会严格按“当前会话的授权源”执行。  
  怎么用：在会话里勾选数据源，或新建会话后补齐源。  
  下一步：进入“数据连接”校验数据结构和质量。

> 备注：文档中的“我是谁（owner/editor/viewer）”差异会影响部分操作，owner 有更多审批与治理权限。

## 二、推荐闭环（按这个顺序操作，成功率高）

1. 数据连接 → 2. 业务口径 → 3. 指标治理 → 4. 智能分析 → 5. 结果复核/交付 → 6. 自动化复用

下面每个功能都按这个闭环说明“功能用途 / 操作路径 / 下一步”。

---

## 三、功能手册

### 1. 智能分析（主入口：**智能分析**）

**用途**  
把“业务问题”转成可执行分析任务：先形成需求契约，再由 Agent 进行查询、分析、验证、发布。

**怎么用**

1. 在输入框写清楚问题（如“分析最近30天订单趋势并定位异常门店”）。  
2. 系统会先产生“需求合同”，你在合同页补齐四项：  
   - 业务目标  
   - 覆盖范围  
   - 数据看问题的维度  
   - 期望交付形式  
3. 点击确认并开始分析。  
4. 跟踪执行状态：  
   - 运行中可暂停 / 继续 / 取消。  
   - 任务页会持续显示事件流（可追溯）。  
5. 完成后查看三类结果：  
   - 极简结论  
   - 仪表盘（图表+授权明细分页）  
   - 完整报告（含口径、归因、限制）  
6. 需要后续动作，使用：
   - 继续追问 / 刷新 / 复现 / 重新分析（branch）

**为什么这样做**  
合同先确认是为了固定“可交付边界”，减少模型跑偏和复盘成本。事件流和证据让结果可解释、可回溯。

**做完这一步，下一步**  
生成成果包（Word/PNG）并走交付；或创建工作流沉淀为标准化流程。

---

### 2. 数据连接（主入口：**数据连接**）

**用途**  
建立可靠的数据资产入口：文件、数据库、HTTP、Google Sheet、飞书多维表。

**怎么用**

1. **上传文件**：CSV/TSV/XLSX/JSON/Parquet。  
2. **或新增外部连接**：数据库 / HTTP / Sheets / 飞书表。  
3. 在右侧列表选中源后执行：  
   - 数据预览（字段、表名、行列数）  
   - 数据画像（缺失、重复、分布）  
   - SQL 控制台（只读）  
   - 数据处理（清洗生成派生源，原始数据不动）  
4. 在“用于当前会话”勾选后，数据会进入该会话分析范围。  
5. 需要复用同一批源可保存为“数据组合”。

**为什么这样做**  
先验检查和只读查询可避免误改源数据；数据组合可保证分析会话可复现。

**做完这一步，下一步**  
去“业务口径”把问题语义化；再回到“指标治理”形成标准指标。

---

### 3. 业务口径（主入口：**业务口径**）

**用途**  
沉淀知识与口径：文档、指标、规则、背景说明，供分析时检索和引用。

**怎么用**

1. 上传业务文档（TXT/MD/HTML/CSV/JSON/PDF/DOCX/XLSX）。  
2. 结构化解析时先预览编辑，确认后再点击“确认并建立索引”。  
3. 在“指标·规则·背景”里增删改条目。  
4. 选“检索验收”检查召回是否命中正确口径。  
5. 需要本会话特殊指令时，设置“会话临时指令”。

**为什么这样做**  
把业务语境放进知识层，可减少模型臆测，提升回答一致性。

**做完这一步，下一步**  
回到数据连接确认字段含义，再在“指标治理”把指标绑定到实际字段。

---

### 4. 指标治理（主入口：**指标治理**）

**用途**  
把“业务口径”变成技术可执行对象：语义模型 + 指标版本 + 审批。

**怎么用**

1. 新建语义模型：选择数据源与表，生成维度/度量草稿。  
2. 新建业务指标：绑定语义模型中的度量，补齐单位、格式、口径说明。  
3. 对指标执行“口径验收”：设置分组、过滤，跑一次核验。  
4. 需要正式可直接调用时审批指标为 `approved`（通常 owner 操作）。

**为什么这样做**  
只有版本化、经过审批的指标才能形成稳定 SQL 与可追溯口径，支持生产交付。

**做完这一步，下一步**  
在“智能分析”引用这些指标执行正式问题分析（结果可复验更稳）。

---

### 5. 报告工厂（主入口：**报告工厂**）

**用途**  
把可复用分析固化为工作流、协作组、定时与事件触发链路。

**怎么用**

- **工作流**：新建 JSON 流程（查询→审批→交付），保存后校验、发布、运行。  
- **运行与审批**：查看执行状态，遇到等待审批可通过/拒绝。  
- **多顾问协作**：建协作组并发起任务。  
- **定时计划**：按 Cron 调度已发布工作流。  
- **事件 Hook**：在完成/失败等事件触发 Webhook 或联动首个工作流。  
- **后台任务**：查看大任务与队列状态。

**为什么这样做**  
一次手工方案可复制为批量标准化流程，提升稳定性、减少重复劳动。

**做完这一步，下一步**  
将关键运行结果汇总到“决策看板”，或对外自动推送。

---

### 6. 决策看板（主入口：**决策看板**）

**用途**  
把图表按业务视角组织为可复用、可刷新、可导出的看板。

**怎么用**

1. 点击“创建看板”，输入名称和说明。  
2. 系统默认组合当前空间最近保存的图表（最多 6 个）作为组件。  
3. 在看板详情里点击“刷新数据”更新，或“导出 HTML”生成交付文件。  
4. 不再使用时可归档看板。

**为什么这样做**  
看板是对结果展示层的治理；同一分析结果可多次复用，不必每次重跑图表。

**做完这一步，下一步**  
如需自动触达外部（飞书/邮件）或内嵌到流程，继续看协同入口/自动化。

---

### 7. 协同入口（主入口：**协同入口**）

**用途**  
把当前会话与飞书群打通，群内消息可回到同一会话上下文执行。

**怎么用**

1. 配置 App ID / App Secret / 验证 Token，保存后启用。  
2. 读取可见群后选择目标群。  
3. 绑定当前会话到群，支持连接 / 断开。  
4. 发起测试消息验证链路。

**为什么这样做**  
确保决策沟通与分析上下文一致，避免“群里提问-会话里重复复现”的信息断层。

**做完这一步，下一步**  
将群内反馈整理到“知识库”或“会话临时指令”中，形成后续分析输入。

---

### 8. 产品总览（主入口：**产品总览**）

**用途**  
看当前租户开通状态、可用功能、方法论和方案包；owner 可调整订阅与配额参数。

**怎么用**

1. 查看当前计划、已开通能力和开通进度。  
2. 对照“标准方法论”与“解决方案”对齐交付阶段。  
3. owner 进入商业配置可调整套餐/状态（需谨慎提交）。  

**为什么这样做**  
把“能做什么”和“客户购买边界”统一可见，避免到执行阶段才发现权限不足。

**做完这一步，下一步**  
按系统状态继续上述业务流程；权限不够时先补齐后再建分析链路。

---

### 9. 系统设置（主入口：**管理设置**）

**用途**  
管理企业级外联与治理能力：模型、MCP、通知、技能、记忆、审计、算力状态。

**怎么用**

- **模型服务**：新增 OpenAI-Compatible 服务，填写 key 后测试连通。  
- **MCP 工具服务**：配置 stdio / HTTP / SSE / Streamable HTTP，点击测试发现工具。  
- **通知连接**：配置 Webhook、飞书、Slack、钉钉、SMTP 并测试发送。  
- **长期记忆**：记录稳定事实和偏好，支持编辑与归档。  
- **分析技能**：创建候选版、测试、审批发布、停用/回滚。  
- **审计日志**：按需回看关键操作与系统行为记录。  

**为什么这样做**  
这是平台运行的“底座控制面”，直接影响模型可用性、工具权限和交付稳定性。

**做完这一步，下一步**  
回到“智能分析”验证模型+工具路径畅通。

---

## 四、辅助能力（经常配合使用）

- **会话管理**（侧边栏）  
  - 新建/切换会话：每个会话可用于一个独立分析任务。  
  - 清空上下文：保留数据和资产，清理历史对话。  
- **命令面板**（`⌘K`）  
  - 快速跳转与快捷命令（如新建会话、打开数据源、查看运行队列等）。  
- **运行中心（左侧/全局）**  
  - 统一查看后台任务进度与异常。

---

## 五、每个模块的“下一步联动”快照

- 数据连接确认后 → 业务口径 → 指标治理 → 智能分析  
- 智能分析完成后 → 生成成果包 → 邮件发送 / 看板生成  
- 重复出现的分析 → 报告工厂工作流化  
- 团队协作上量后 → 协同入口绑定 + 自动化触发  

---

## 六、下一步建议（直接照着执行）

1. 按“工作空间 → 数据连接 → 指标治理”完成一次最小链路。  
2. 在智能分析发起一个问题并确认合同。  
3. 产出 1 个可复现结论和 1 个可导出文件。  
4. 把这个过程封成 1 个工作流（至少：查询 + 审批 + 导出）。  
5. 把关键反馈沉淀到知识库，形成版本化口径。  

如果你愿意，我下一步可以再给你补一版“**一人操作 30 分钟 SOP**”（按按钮级步骤清单），用于直接给同事培训。
